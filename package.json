{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel --stream dev",
    "start:projeto1": "lerna exec --stream --scope @ds/projeto1 yarn dev",
    "start:projeto2": "lerna exec --stream --scope @ds/projeto2 yarn dev",
    "build": "lerna run --parallel build",
    "lint": "lerna run --parallel lint"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
