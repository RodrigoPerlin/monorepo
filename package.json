{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel --stream dev",
    "start:components": "lerna exec --stream --scope @ds/components yarn dev",
    "build": "lerna run --parallel build",
    "lint": "lerna run --parallel lint"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
